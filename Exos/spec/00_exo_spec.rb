require_relative '../lib/00_exo'

describe "longueur method" do
    it "should put the length of the hash when asked" do
      expect(longueur()).to eq(20)
    end
    end

describe "verification method" do
    it "should put BTC when asked" do
        expect(verification()).to eq(true)
    end
    end

